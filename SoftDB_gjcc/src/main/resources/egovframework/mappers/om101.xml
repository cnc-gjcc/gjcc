<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="om101">
	
	<select id = "selectList" resultType="map" parameterType="map">
		select
				dept_id
			,	dept_name
			,	ext_num
		from om101
	</select>
</mapper>